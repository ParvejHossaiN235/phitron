// Useful math functions

#include<stdio.h>
#include <math.h>

int main()
{
    // int ans = ceil(4.5);
     int ans = floor(4.5);
    // int ans = round(5.5);
    // int ans = sqrt(25);
    // int ans = pow(5, 3);  // 1st value er power hoi 
    // int ans = abs(-25);
    
    printf("%d", ans);
    return 0;
}