#include <stdio.h>

// additional oparator
int main () 
{

    int a = 20;
    int b = 10;

    int sum = a + b;
    printf("sumation = %d\n", sum);

    int sub = a - b;
    printf("subtraction = %d\n", sub);

    int multi = b * a;
    printf("multipliction = %d\n", multi );


    int divis = a / b;
    printf("division = %d\n", divis);

    float c = 23;
    int d = 3;

    float divi = c / d;

    printf("division percentage value = %f", divi);

    return 0;
}